# Written by tamp when this environment was created.
#
# tamp never runs 'git init' — whether this directory is a repository is your
# call. If it is one, these are the paths that should stay out of it: tamp
# regenerates them from tamp.toml, and .tamp/ holds this machine's secrets
# and snapshots.
/.tamp/
/compose.yaml
/.devcontainer/
